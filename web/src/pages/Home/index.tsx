import { Header } from '../../components/Header';
import {
    HomeContainer,
} from './styles';

export const Home = () => {

    return (
        <HomeContainer>
            <Header />

        </HomeContainer>
    );
}